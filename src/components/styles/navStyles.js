import { makeStyles } from "@material-ui/styles";

export const navStyles = makeStyles({
  navwrap: {
    background: "#000000",
    borderBottom: "2px solid #83d7d1",
    borderRadius: 5,
    height: 30
  },
  navtext: {
    color: "red"
  }
});
